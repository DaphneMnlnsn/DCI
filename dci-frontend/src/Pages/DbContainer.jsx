                        {/*<table className='database-table'>
                            <thead>
                                {database.map((data, index) => (
                                    <tr key={index}>
                                        <th>{data.masters}</th>
                                    </tr>
                                ))}
                            </thead>
                            <tbody className='database-body'>
                                {database.map((data, index) => (
                                    <tr key={index}>
                                        <td>{data.columns}</td>
                                    </tr>
                                ))}  
                            </tbody> 
                        </table>*/}